ImageZoom 复合组件。

<ComponentShowcase id="image-zoom" path="image-zoom/basic" />

```tsx
import { ImageZoom } from "@/components/ui/image-zoom"

export default function Demo() {
  return <ImageZoom className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/image-zoom/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/image-zoom` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="image-zoom" />
