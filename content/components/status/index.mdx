Status 复合组件。

<ComponentShowcase id="status" path="status/basic" />

```tsx
import { Status } from "@/components/ui/status"

export default function Demo() {
  return <Status className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/status/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/status` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="status" />
