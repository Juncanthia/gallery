CalendarBody 复合组件。

<ComponentShowcase id="feature-calendar" path="feature-calendar/basic" />

```tsx
import { CalendarBody } from "@/components/ui/feature-calendar"

export default function Demo() {
  return <CalendarBody className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/feature-calendar/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/feature-calendar` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="feature-calendar" />
