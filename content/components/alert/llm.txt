---
category: Components
title: Alert
subtitle: 警告提示
description: 警告提示，展现需要关注的信息。
demo:
  cols: 2
group:
  title: 反馈
  order: 6
source:
  component: src/components/ui/alert.tsx
  examples:
    - content/components/alert/examples/basic.tsx
stack:
  react: true
  tailwindcss: true
  cva: true
  radixSlot: false
  motion: false
  baseUi: false
  sonner: false
  rcComponent: false
  cssInJs: false
---

# Alert 警告提示

警告提示，展现需要关注的信息。

本组件借鉴 Ant Design Alert 的使用心智，但不是 AntD 兼容层。实现以本地源码为准，不引入 AntD 的 ConfigProvider、css-in-js、Design Token 运行时或历史兼容语法糖。

## 何时使用 {#when-to-use}

适合在需要 警告提示 相关能力的界面中使用。优先通过组件自身 props 表达状态和语义，避免在业务侧堆叠重复 className。

常见场景：

- 需要复用统一设计语言、状态样式和可访问性约定。
- 需要和本项目 Tailwind 主题变量、暗色模式、Radix/Base UI primitive 或本地交互行为保持一致。
- 需要让 AI 或业务代码稳定地从 `@/components/ui/alert` 引用组件，而不是直接引用第三方底层库。

不推荐：

- 为了照搬 Ant Design API 而新增兼容别名。
- 在调用侧绕过组件 API，直接依赖内部 DOM 结构完成业务逻辑。
- 引入 `antd`、`@ant-design/icons`、`antd-style` 或 rc-component 作为使用该组件的前置条件。

## 代码演示 {#examples}

### 基础用法

```tsx
import { Alert } from "@/components/ui/alert"

export default function Demo() {
  return (
    <Alert>
      警告提示
    </Alert>
  )
}
```

> 说明：这是面向 LLM 的最小导入示例。复杂组件应结合下方 API 摘要和源码类型补充必需 props，例如 `options`、`items`、`columns`、`dataSource` 等。

## 核心导出 {#exports}

- `Alert`
- `AlertAction`
- `AlertClosable`
- `AlertDescription`
- `AlertProps`
- `AlertTitle`
- `AlertType`
- `AlertVariant`

## API 摘要 {#api}

以下类型来自本地源码，是生成代码时的主要依据：

```ts
type AlertType = "success" | "info" | "warning" | "error"
```

```ts
type AlertVariant = "outlined" | "filled"
```

```ts
type AlertClosable =
  | boolean
  | {
      closeIcon?: React.ReactNode
      onClose?: React.MouseEventHandler<HTMLButtonElement>
      afterClose?: () => void
    }
```

```ts
type AlertProps = Omit<React.ComponentProps<"div">, "title"> &
  Omit<VariantProps<typeof alertVariants>, "variant"> & {
    type?: AlertType
    variant?: AlertVariant | NonNullable<VariantProps<typeof alertVariants>["variant"]>
    title?: React.ReactNode
    message?: React.ReactNode
    description?: React.ReactNode
    icon?: React.ReactNode
    showIcon?: boolean
    action?: React.ReactNode
    closable?: AlertClosable
    closeText?: React.ReactNode
    closeIcon?: React.ReactNode
    onClose?: React.MouseEventHandler<HTMLButtonElement>
    afterClose?: () => void
    banner?: boolean
  }
```

## Semantic DOM {#semantic-dom}

源码中出现的稳定语义槽位：

- `alert`
- `alert-action`
- `alert-description`
- `alert-title`

额外状态属性：

- `data-slot`

生成测试、样式定位或自动化脚本时，优先使用这些语义属性；不要依赖易变的 Tailwind class 拼接顺序。

## 样式模型 {#styling-model}

- 样式主要由 Tailwind CSS className 与本地 `cn` 合并函数控制。
- 如果源码使用 CVA，则变体样式集中在组件文件或相邻 `*-variants.ts` 中。
- 如果源码包装 Radix/Base UI primitive，则交互状态、键盘行为和 ARIA 基础能力来自底层 primitive，本地组件负责视觉和 API 收敛。
- 主题颜色优先使用 `bg-background`、`text-foreground`、`border-border`、`text-muted-foreground`、`ring-ring` 等项目级语义变量。

## 依赖与实现说明 {#implementation-notes}

核心文件：

- `src/components/ui/alert.tsx`

直接依赖摘要：

- `class-variance-authority`

实现注意：

1. 组件 API 以本地 TypeScript 类型为准。
2. 样式应优先通过组件 props、variant 或本地 className 扩展点表达。
3. 如果需要新增能力，应先修改该组件源码和相邻 variant 文件，再更新本 llm 文档。
4. 不要把第三方文档中的全量 API 视为本地已支持能力。

## 可访问性 {#accessibility}

- 保留原生元素或底层 primitive 的键盘语义。
- 交互型组件应提供可访问名称，例如 `aria-label`、`title` 或可见文本。
- 禁用、加载、展开、选中等状态应优先使用组件已有 props，而不是只改视觉 class。
- 纯图标触发器必须提供明确的可访问名称。

## 与 Ant Design 的取舍 {#antd-differences}

| 方向 | 本地决策 |
| --- | --- |
| API 来源 | 以 `src/components/ui/alert.tsx` 的 TypeScript 类型为准 |
| 视觉系统 | 使用 Tailwind CSS 和项目主题变量 |
| 全局配置 | 不依赖 AntD ConfigProvider |
| 样式运行时 | 不引入 AntD css-in-js 或 token 运行时 |
| 兼容目标 | 借鉴 AntD 使用心智，但不承诺 API 兼容 |

## LLM 使用规则 {#llm-rules}

当 AI 生成本项目 Alert 代码时，应遵守：

1. 从 `@/components/ui/alert` 导入，不要从 `antd` 导入。
2. 只使用本文件 API 摘要和源码中存在的 props。
3. 不要生成 AntD 专属的 `classNames`、`styles`、`ConfigProvider`、Design Token 配置，除非本地源码明确支持。
4. 需要自定义样式时，优先使用组件暴露的 props；最后才使用 `className`。
5. 交互组件必须保留键盘和 ARIA 语义，纯图标操作必须补 `aria-label`。
6. 如果不确定某个 AntD API 是否存在，先检查 `src/components/ui/alert.tsx`，不要凭 AntD 记忆生成。
