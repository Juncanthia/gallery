---
category: Components
title: DropdownMenu
subtitle: DropdownMenu
description: DropdownMenu 是本地基础组件。
demo:
  cols: 2
group:
  title: 通用
  order: 1
source:
  component: src/components/core/dropdown-menu.tsx
  examples:
    - content/components/dropdown-menu/examples/basic.tsx
stack:
  react: true
  tailwindcss: true
  cva: false
  radixSlot: true
  motion: false
  baseUi: false
  sonner: false
  rcComponent: false
  cssInJs: false
---

# DropdownMenu DropdownMenu

DropdownMenu 是本地基础组件。

本组件是项目内的本地基础能力，API 和行为以源码为准，不对应 Ant Design 的完整公共组件契约。

## 何时使用 {#when-to-use}

适合在需要 DropdownMenu 相关能力的界面中使用。优先通过组件自身 props 表达状态和语义，避免在业务侧堆叠重复 className。

常见场景：

- 需要复用统一设计语言、状态样式和可访问性约定。
- 需要和本项目 Tailwind 主题变量、暗色模式、Radix/Base UI primitive 或本地交互行为保持一致。
- 需要让 AI 或业务代码稳定地从 `@/components/core/dropdown-menu` 引用组件，而不是直接引用第三方底层库。

不推荐：

- 为了照搬 Ant Design API 而新增兼容别名。
- 在调用侧绕过组件 API，直接依赖内部 DOM 结构完成业务逻辑。
- 引入 `antd`、`@ant-design/icons`、`antd-style` 或 rc-component 作为使用该组件的前置条件。

## 代码演示 {#examples}

### 基础用法

```tsx
import { DropdownMenu } from "@/components/core/dropdown-menu"

export default function Demo() {
  return (
    <DropdownMenu>
      DropdownMenu
    </DropdownMenu>
  )
}
```

> 说明：这是面向 LLM 的最小导入示例。复杂组件应结合下方 API 摘要和源码类型补充必需 props，例如 `options`、`items`、`columns`、`dataSource` 等。

## 核心导出 {#exports}

- `DropdownMenu`
- `DropdownMenuCheckboxItem`
- `DropdownMenuCheckboxItemProps`
- `DropdownMenuContent`
- `DropdownMenuContentProps`
- `DropdownMenuGroup`
- `DropdownMenuGroupProps`
- `DropdownMenuItem`
- `DropdownMenuItemOption`
- `DropdownMenuItemProps`
- `DropdownMenuLabel`
- `DropdownMenuLabelProps`
- `DropdownMenuPlacement`
- `DropdownMenuProps`
- `DropdownMenuRadioGroup`
- `DropdownMenuRadioGroupProps`
- `DropdownMenuRadioItem`
- `DropdownMenuRadioItemProps`
- `DropdownMenuSeparator`
- `DropdownMenuSeparatorProps`
- `DropdownMenuShortcut`
- `DropdownMenuShortcutProps`
- `DropdownMenuSub`
- `DropdownMenuSubContent`
- `DropdownMenuSubContentProps`
- `DropdownMenuSubProps`
- `DropdownMenuSubTrigger`
- `DropdownMenuSubTriggerProps`
- `DropdownMenuTrigger`
- `DropdownMenuTriggerProps`

## API 摘要 {#api}

以下类型来自本地源码，是生成代码时的主要依据：

```ts
type DropdownMenuPlacement =
  | 'top'
  | 'bottom'
  | 'topLeft'
  | 'topRight'
  | 'bottomLeft'
  | 'bottomRight'
  | 'left'
  | 'leftTop'
  | 'leftBottom'
  | 'right'
  | 'rightTop'
  | 'rightBottom';
```

```ts
type DropdownMenuPlacementConfig = Pick<DropdownMenuContentProps, 'align' | 'side'>;
```

```ts
type DropdownMenuItemOption =
  | {
      key?: React.Key;
      type: 'label';
      label: React.ReactNode;
      inset?: boolean;
    }
  | {
      key?: React.Key;
      type: 'separator';
    }
  | {
      key?: React.Key;
      type?: 'item';
      label: React.ReactNode;
      icon?: React.ReactNode;
      extra?: React.ReactNode;
      shortcut?: React.ReactNode;
      disabled?: boolean;
      danger?: boolean;
      onSelect?: DropdownMenuItemPrimitiveProps['onSelect'];
    }
  | {
      key?: React.Key;
      type: 'checkbox';
      label: React.ReactNode;
      checked?: boolean;
      disabled?: boolean;
      onCheckedChange?: DropdownMenuCheckboxItemPrimitiveProps['onCheckedChange'];
    }
  | {
      key?: React.Key;
      type: 'submenu';
      label: React.ReactNode;
      icon?: React.ReactNode;
      extra?: React.ReactNode;
      disabled?: boolean;
      children: DropdownMenuItemOption[];
    };
```

```ts
type DropdownMenuProps = DropdownMenuPrimitiveProps & {
  trigger?: React.ReactNode;
  items?: DropdownMenuItemOption[];
  menu?: { items?: DropdownMenuItemOption[] };
  placement?: DropdownMenuPlacement;
  contentProps?: DropdownMenuContentProps;
};
```

```ts
type DropdownMenuTriggerProps = DropdownMenuTriggerPrimitiveProps;
```

```ts
type DropdownMenuContentProps = DropdownMenuContentPrimitiveProps;
```

```ts
type DropdownMenuGroupProps = DropdownMenuGroupPrimitiveProps;
```

```ts
type DropdownMenuItemProps = DropdownMenuItemPrimitiveProps & {
  inset?: boolean;
  variant?: 'default' | 'destructive';
};
```

```ts
type DropdownMenuCheckboxItemProps = DropdownMenuCheckboxItemPrimitiveProps;
```

```ts
type DropdownMenuRadioGroupProps = DropdownMenuRadioGroupPrimitiveProps;
```

## Semantic DOM {#semantic-dom}

源码中出现的稳定语义槽位：

- `chevron`

额外状态属性：

- `data-inset`
- `data-slot`
- `data-variant`

生成测试、样式定位或自动化脚本时，优先使用这些语义属性；不要依赖易变的 Tailwind class 拼接顺序。

## 样式模型 {#styling-model}

- 样式主要由 Tailwind CSS className 与本地 `cn` 合并函数控制。
- 如果源码使用 CVA，则变体样式集中在组件文件或相邻 `*-variants.ts` 中。
- 如果源码包装 Radix/Base UI primitive，则交互状态、键盘行为和 ARIA 基础能力来自底层 primitive，本地组件负责视觉和 API 收敛。
- 主题颜色优先使用 `bg-background`、`text-foreground`、`border-border`、`text-muted-foreground`、`ring-ring` 等项目级语义变量。

## 依赖与实现说明 {#implementation-notes}

核心文件：

- `src/components/core/dropdown-menu.tsx`

直接依赖摘要：

- `@/components/core/button`
- `@/components/core/dropdown-menu`

实现注意：

1. 组件 API 以本地 TypeScript 类型为准。
2. 样式应优先通过组件 props、variant 或本地 className 扩展点表达。
3. 如果需要新增能力，应先修改该组件源码和相邻 variant 文件，再更新本 llm 文档。
4. 不要把第三方文档中的全量 API 视为本地已支持能力。

## 可访问性 {#accessibility}

- 保留原生元素或底层 primitive 的键盘语义。
- 交互型组件应提供可访问名称，例如 `aria-label`、`title` 或可见文本。
- 禁用、加载、展开、选中等状态应优先使用组件已有 props，而不是只改视觉 class。
- 纯图标触发器必须提供明确的可访问名称。

## 与 Ant Design 的取舍 {#antd-differences}

| 方向 | 本地决策 |
| --- | --- |
| API 来源 | 以 `src/components/core/dropdown-menu.tsx` 的 TypeScript 类型为准 |
| 视觉系统 | 使用 Tailwind CSS 和项目主题变量 |
| 全局配置 | 不依赖 AntD ConfigProvider |
| 样式运行时 | 不引入 AntD css-in-js 或 token 运行时 |
| 兼容目标 | 借鉴 AntD 使用心智，但不承诺 API 兼容 |

## LLM 使用规则 {#llm-rules}

当 AI 生成本项目 DropdownMenu 代码时，应遵守：

1. 从 `@/components/core/dropdown-menu` 导入，不要从 `antd` 导入。
2. 只使用本文件 API 摘要和源码中存在的 props。
3. 不要生成 AntD 专属的 `classNames`、`styles`、`ConfigProvider`、Design Token 配置，除非本地源码明确支持。
4. 需要自定义样式时，优先使用组件暴露的 props；最后才使用 `className`。
5. 交互组件必须保留键盘和 ARIA 语义，纯图标操作必须补 `aria-label`。
6. 如果不确定某个 AntD API 是否存在，先检查 `src/components/core/dropdown-menu.tsx`，不要凭 AntD 记忆生成。
