Transfer 双栏穿梭选择框，在两侧列表间移动选项。

<ComponentShowcase id="transfer" path="transfer/basic" />

```tsx
import { Transfer } from "@/components/ui/transfer"
export default function Demo() { return <Transfer dataSource={[{ key: "1", title: "项目一" }, { key: "2", title: "项目二" }]} targetKeys={[]} className="max-w-md" /> }
```

<h2 id="api">API</h2>
| 属性 | 说明 | 类型 |
| --- | --- | --- |
| dataSource | 数据源 | `TransferItem[]` |
| targetKeys | 右侧选中项 | `string[]` |
| onChange | 变化回调 | `(targetKeys) => void` |

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/transfer` 导入。


<h2 id="semantic-dom">Semantic DOM</h2>

组件使用原生元素语义，未声明额外的 `data-slot` 属性。通过标签名和 ARIA 属性定位。

<h2 id="source">源码</h2>
<SourceViewer componentId="transfer" />
