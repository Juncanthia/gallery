GanttHeader 复合组件。

<ComponentShowcase id="gantt" path="gantt/basic" />

```tsx
import { GanttHeader } from "@/components/ui/gantt"

export default function Demo() {
  return <GanttHeader className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/gantt/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/gantt` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="gantt" />
