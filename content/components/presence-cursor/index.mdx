Cursor 复合组件。

<ComponentShowcase id="presence-cursor" path="presence-cursor/basic" />

```tsx
import { Cursor } from "@/components/ui/presence-cursor"

export default function Demo() {
  return <Cursor className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/presence-cursor/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/presence-cursor` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="presence-cursor" />
