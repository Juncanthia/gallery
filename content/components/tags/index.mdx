Tags 复合组件。

<ComponentShowcase id="tags" path="tags/basic" />

```tsx
import { Tags } from "@/components/ui/Tags"

export default function Demo() {
  return <Tags className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/Tags/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/Tags` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="Tags" />
