DialogStack 复合组件。

<ComponentShowcase id="dialog-stack" path="dialog-stack/basic" />

```tsx
import { DialogStack } from "@/components/ui/dialog-stack"

export default function Demo() {
  return <DialogStack className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/dialog-stack/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/dialog-stack` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="dialog-stack" />
