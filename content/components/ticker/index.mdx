Ticker 复合组件。

<ComponentShowcase id="ticker" path="ticker/basic" />

```tsx
import { Ticker } from "@/components/ui/ticker"

export default function Demo() {
  return <Ticker className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/ticker/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/ticker` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="ticker" />
