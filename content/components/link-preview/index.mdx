PreviewLinkCard 链接预览组件，hover 时展示链接内容的摘要卡片。

<ComponentShowcase id="link-preview" path="link-preview/basic" />

```tsx
import { PreviewLinkCard, PreviewLinkCardTrigger, PreviewLinkCardContent } from "@/components/ui/link-preview"

export default function Demo() {
  return (
    <PreviewLinkCard className="w-64">
      <PreviewLinkCardTrigger href="https://example.com" className="text-sm underline">
        悬停查看预览
      </PreviewLinkCardTrigger>
      <PreviewLinkCardContent>
        <div className="text-sm">链接预览卡片内容</div>
      </PreviewLinkCardContent>
    </PreviewLinkCard>
  )
}
```

<h2 id="api">API</h2>

子组件：`PreviewLinkCard`、`PreviewLinkCardTrigger`、`PreviewLinkCardContent`、`PreviewLinkCardImage`。

<h2 id="semantic-dom">Semantic DOM</h2>

组件使用原生元素语义，通过标签名和 ARIA 属性定位。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/link-preview` 导入 `PreviewLinkCard`。

<h2 id="source">源码</h2>
<SourceViewer componentId="link-preview" />
