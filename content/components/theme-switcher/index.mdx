ThemeSwitcher 复合组件。

<ComponentShowcase id="theme-switcher" path="theme-switcher/basic" />

```tsx
import { ThemeSwitcher } from "@/components/ui/theme-switcher"

export default function Demo() {
  return <ThemeSwitcher className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/theme-switcher/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/theme-switcher` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="theme-switcher" />
