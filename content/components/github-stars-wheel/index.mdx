GitHubStarsWheel GitHub 星标数字滚轮组件，自动获取 GitHub 仓库星标数并展示滚动数字动画效果。星标取整后以数字滚轮形式滚动，滚动完成时星形图标填充金色并触发粒子特效。

<h2 id="when-to-use">何时使用</h2>

当需要在页面中展示 GitHub 仓库星标数以增加社交证明时使用。

常见场景：

- 开源项目首页展示星标数。
- 产品 Landing Page 的社会证明模块。
- 开发者作品集展示项目受欢迎程度。
- 按百位/千位取整展示的大数字场景。

不推荐：

- 无 GitHub 仓库关联的纯数字展示——使用 ScrollingNumber 原语即可。
- 需要实时更新的星标数监控——组件仅在挂载时获取一次数据。
- 静态数值展示——直接用文本，不需要滚动动画。

<h2 id="examples">代码演示</h2>

### 基础用法

<ComponentShowcase id="github-stars-wheel" path="github-stars-wheel/basic" />

```tsx
import { GitHubStarsWheel } from "@/components/ui/github-stars-wheel"

export default function Demo() {
  return (
    <GitHubStarsWheel
      username="shadcn-ui"
      repo="ui"
    />
  )
}
```

### 指定 step 和方向

```tsx
import { GitHubStarsWheel } from "@/components/ui/github-stars-wheel"

export default function Demo() {
  return (
    <GitHubStarsWheel
      username="vercel"
      repo="next.js"
      direction="ttb"
      step={1000}
      delay={500}
    />
  )
}
```

### 受控数值模式

传入 `value` 后跳过 API 请求，直接展示固定值。

```tsx
import { GitHubStarsWheel } from "@/components/ui/github-stars-wheel"

export default function Demo() {
  return (
    <GitHubStarsWheel
      value={72500}
      step={100}
      direction="btt"
    />
  )
}
```

<h2 id="api">API</h2>

### GitHubStarsWheel

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| username | GitHub 用户名 | `string` | - |
| repo | GitHub 仓库名 | `string` | - |
| value | 受控数值，传入后跳过 API 获取 | `number` | - |
| direction | 数字滚动方向 | `"btt"` \| `"ttb"` | `"btt"` |
| delay | API 请求延迟（ms） | `number` | `0` |
| step | 数字取整步长（如 100 表示四舍五入到百位） | `number` | `100` |
| itemsSize | 单个数字项高度（px） | `number` | `35` |
| sideItemsCount | 可见的上下侧项数 | `number` | `2` |
| className | 附加 className | `string` | - |

继承 `Omit<ScrollingNumberContainerPrimitiveProps, "direction" | "number" | "step">`。

组件行为说明：

1. 若同时传入 `value` 和 `username`/`repo`，优先使用 `value`，不发起 API 请求。
2. `username` 和 `repo` 任一为空且 `value` 未传入时，不发起 API 请求（初始 stars 为 0）。
3. API 请求在 `delay` 毫秒后发起，使用 `fetch` 调用 `https://api.github.com/repos/{username}/{repo}`。
4. 星标数按 `step` 取整：`Math.round(stars / step) * step`。
5. 数字滚动完毕后，星形图标从灰色填充至金色完整（`clipPath` inset 动画），并触发粒子特效。
6. 加载中不渲染任何内容（`isLoading` 为 true 时返回 null），父容器可设置占位高度避免布局跳动。

<h2 id="faq">常见问题</h2>

### 如何展示精确星标数而不是取整值？

将 `step` 设为 `1`，星标数将直接显示原始值而不进行取整。

```tsx
<GitHubStarsWheel username="shadcn-ui" repo="ui" step={1} />
```

### 如何展示固定星标数而不依赖 GitHub API？

仅传入 `value`，不要同时传入 `username` 和 `repo`，组件将使用指定的固定值并跳过 API 请求。

### 加载过程中组件不显示任何内容怎么办？

加载期间 `isLoading` 为 true 时组件返回 null，这是设计行为。父容器可提前设置固定高度占位以避免布局跳动。如果长时间无内容，检查 GitHub API 是否可访问或 `username`/`repo` 是否正确。

### 星形图标如何变金色？

数字滚动完成（`currentStars === roundedStars`）时，星形图标的金色覆盖层通过 `clipPath: inset` 从 `inset(100% 0 0 0)` 过渡到 `inset(0% 0 0 0)`，实现从底部到顶部的填充动画效果。

<h2 id="llm-rules">LLM 使用规则</h2>

1. 从 `@/components/ui/github-stars-wheel` 导入 `GitHubStarsWheel`（命名导出）。
2. 如需自动获取真实星标数，必须同时传入 `username` 和 `repo`；否则组件将显示 `0`。
3. 如需固定星标数不发起 API 请求，仅传入 `value`，不要同时传入 `username`/`repo`。
4. `step` 默认值为 `100`，星标数会四舍五入到百位；如需精确值，将 `step` 设为 `1`。
5. `direction` 仅在需要反向滚动时显式传入 `"ttb"`，绝大多数场景使用默认的 `"btt"`（从下向上）即可。
6. 组件加载期间返回 null，父容器可提前设置占位高度避免布局跳动。
7. 不要将本组件用于展示非 GitHub 来源的数据——星形图标和 GitHub API 调用是硬编码的。
8. 不要引入 Ant Design 相关依赖。

<h2 id="source">源码</h2>
<SourceViewer componentId="github-stars-wheel" />
