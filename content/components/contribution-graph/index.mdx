ContributionGraph 复合组件。

<ComponentShowcase id="contribution-graph" path="contribution-graph/basic" />

```tsx
import { ContributionGraph } from "@/components/ui/contribution-graph"

export default function Demo() {
  return <ContributionGraph className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/contribution-graph/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/contribution-graph` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="contribution-graph" />
