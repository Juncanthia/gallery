RelativeTime 复合组件。

<ComponentShowcase id="relative-time" path="relative-time/basic" />

```tsx
import { RelativeTime } from "@/components/ui/relative-time"

export default function Demo() {
  return <RelativeTime className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/relative-time/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/relative-time` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="relative-time" />
