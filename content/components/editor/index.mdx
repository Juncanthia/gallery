Editor 富文本编辑器。基于 Tiptap 的完整编辑体验，内置斜杠命令、气泡菜单、浮动菜单、表格编辑、字数统计等。

<h2 id="when-to-use">何时使用</h2>

当需要为应用提供所见即所得的富文本编辑能力时使用。

常见场景：

- 内容管理系统（CMS）的文章/页面编辑器。
- 评论、笔记、文档编辑。
- 需要表格编辑、代码块、任务列表的协作工具。
- 支持 Markdown 快捷输入（输入 `/` 触发斜杠命令）。
- 需要字符计数限制的长文本输入。

不推荐：

- 绕过 EditorProvider 直接使用底层 `@tiptap/react`，除非需要完全自定义扩展配置。
- 简单文本输入场景——用 Textarea 或 Input 更轻量。
- 纯代码编辑——使用 CodeMirror 或 Monaco 更合适。

<h2 id="examples">代码演示</h2>

### 基础用法

<ComponentShowcase id="editor" path="editor/basic" />

```tsx
import { EditorProvider } from "@/components/ui/editor"

export default function Demo() {
  return <EditorProvider className="w-full max-w-sm" />
}
```

### 完整工具栏

组合 EditorBubbleMenu、EditorSelector、EditorLinkSelector 及各类格式/节点按钮构建完整格式化工具栏。

```tsx
import {
  EditorBubbleMenu,
  EditorFormatBold,
  EditorFormatItalic,
  EditorFormatStrike,
  EditorFormatCode,
  EditorLinkSelector,
  EditorNodeHeading1,
  EditorNodeHeading2,
  EditorNodeHeading3,
  EditorNodeBulletList,
  EditorNodeOrderedList,
  EditorNodeQuote,
  EditorNodeCode,
  EditorProvider,
  EditorSelector
} from "@/components/ui/editor"

export default function Demo() {
  return (
    <EditorProvider className="w-full max-w-sm">
      <EditorBubbleMenu>
        <EditorSelector title="Text">
          <EditorNodeText />
          <EditorNodeHeading1 />
          <EditorNodeHeading2 />
          <EditorNodeHeading3 />
          <EditorNodeBulletList />
          <EditorNodeOrderedList />
          <EditorNodeQuote />
          <EditorNodeCode />
        </EditorSelector>
        <EditorFormatBold hideName />
        <EditorFormatItalic hideName />
        <EditorFormatStrike hideName />
        <EditorFormatCode hideName />
        <EditorLinkSelector />
      </EditorBubbleMenu>
    </EditorProvider>
  )
}
```

### 表格编辑

使用 EditorTableMenu 系列组件为表格提供列级、行级和全局上下文工具栏。

```tsx
import {
  EditorProvider,
  EditorTableColumnMenu,
  EditorTableColumnBefore,
  EditorTableColumnAfter,
  EditorTableColumnDelete,
  EditorTableRowMenu,
  EditorTableRowBefore,
  EditorTableRowAfter,
  EditorTableRowDelete,
  EditorTableGlobalMenu,
  EditorTableHeaderColumnToggle,
  EditorTableHeaderRowToggle,
  EditorTableMergeCells,
  EditorTableSplitCell,
  EditorTableDelete,
  EditorTableFix
} from "@/components/ui/editor"

export default function Demo() {
  return (
    <EditorProvider>
      <EditorTableColumnMenu>
        <EditorTableColumnBefore />
        <EditorTableColumnAfter />
        <EditorTableColumnDelete />
      </EditorTableColumnMenu>
      <EditorTableRowMenu>
        <EditorTableRowBefore />
        <EditorTableRowAfter />
        <EditorTableRowDelete />
      </EditorTableRowMenu>
      <EditorTableGlobalMenu>
        <EditorTableHeaderColumnToggle />
        <EditorTableHeaderRowToggle />
        <EditorTableMergeCells />
        <EditorTableSplitCell />
        <EditorTableDelete />
        <EditorTableFix />
      </EditorTableGlobalMenu>
    </EditorProvider>
  )
}
```

<h2 id="api">API</h2>

### EditorProvider

顶层编辑器提供者，封装 `TiptapEditorProvider`，注册默认扩展集。所有编辑器和菜单子组件必须放在其内部。

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| className | 附加 className | `string` | - |
| limit | 字符计数上限 | `number` | - |
| placeholder | 空编辑器占位文本 | `string` | - |
| extensions | 额外 Tiptap 扩展，合并到默认扩展之后 | `Extension[]` | - |
| content | 初始内容 | `Content`（from @tiptap/react） | - |
| onUpdate | 内容变更回调 | `(props: { editor: Editor }) => void` | - |

默认已注册：StarterKit、Typography、Placeholder、CharacterCount、CodeBlockLowlight（全语言高亮）、Superscript、Subscript、Slash（斜杠命令）、Table、TaskList/TaskItem、TextStyleKit。

### EditorFloatingMenu

浮动菜单，在空行开头显示。

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| className | 附加 className | `string` | - |

继承 `FloatingMenuProps`（不含 editor），editor 从 `useCurrentEditor` 自动获取。

### EditorBubbleMenu

选中文本时弹出的气泡格式菜单。children 为数组时自动在元素间插入 Separator 分隔线。

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| className | 附加 className | `string` | - |
| children | 菜单按钮列表 | `ReactNode \| ReactNode[]` | - |

### EditorSelector

通用节点类型下拉选择器，基于 Popover 实现。

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| title | 触发按钮文字 | `string` | 必填 |
| open | 受控开启状态 | `boolean` | - |
| onOpenChange | 开启状态变化回调 | `(open: boolean) => void` | - |
| className | 附加 className | `string` | - |
| children | 下拉选项，通常为 EditorNode* 组件 | `ReactNode` | - |

### EditorLinkSelector

链接插入/编辑/删除弹窗。

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| open | 受控弹窗开启状态 | `boolean` | - |
| onOpenChange | 弹窗状态变化回调 | `(open: boolean) => void` | - |

### 节点类型按钮

共享 `{ hideName?: boolean }` Props。`hideName` 为 `false` 时同时显示图标和文本。

| 组件 | 功能 |
| --- | --- |
| EditorNodeText | 切换为普通文本段落 |
| EditorNodeHeading1 | 切换为一级标题 |
| EditorNodeHeading2 | 切换为二级标题 |
| EditorNodeHeading3 | 切换为三级标题 |
| EditorNodeBulletList | 切换为无序列表 |
| EditorNodeOrderedList | 切换为有序列表 |
| EditorNodeTaskList | 切换为任务列表 |
| EditorNodeQuote | 切换为引用块 |
| EditorNodeCode | 切换为代码块 |
| EditorNodeTable | 插入 3x3 表格（含表头行） |

### 内联格式按钮

共享 `{ hideName?: boolean }` Props。EditorClearFormatting 的 `hideName` 默认为 `true`。

| 组件 | 功能 |
| --- | --- |
| EditorFormatBold | 切换加粗 |
| EditorFormatItalic | 切换斜体 |
| EditorFormatStrike | 切换删除线 |
| EditorFormatCode | 切换行内代码 |
| EditorFormatSubscript | 切换下标 |
| EditorFormatSuperscript | 切换上标 |
| EditorFormatUnderline | 切换下划线 |
| EditorClearFormatting | 清除所有格式 |

### 表格上下文菜单容器

仅在表格激活时渲染的容器组件。

| 组件 | Props | 定位方式 |
| --- | --- | --- |
| EditorTableMenu | `children: ReactNode` | 仅当 `editor.isActive("table")` 时渲染 |
| EditorTableGlobalMenu | `children: ReactNode` | 表格底部居中浮层 |
| EditorTableColumnMenu | `children: ReactNode` | 列顶部居中，DropdownMenu 触发 |
| EditorTableRowMenu | `children: ReactNode` | 行左侧居中，DropdownMenu 触发 |

### 表格操作按钮

无需自定义 Props，直接使用的功能性按钮组件。

| 组件 | 功能 |
| --- | --- |
| EditorTableColumnBefore | 左侧插入列 |
| EditorTableColumnAfter | 右侧插入列 |
| EditorTableColumnDelete | 删除当前列 |
| EditorTableRowBefore | 上方插入行 |
| EditorTableRowAfter | 下方插入行 |
| EditorTableRowDelete | 删除当前行 |
| EditorTableHeaderColumnToggle | 切换标题列 |
| EditorTableHeaderRowToggle | 切换标题行 |
| EditorTableMergeCells | 合并选中单元格 |
| EditorTableSplitCell | 拆分选中单元格 |
| EditorTableDelete | 删除整个表格 |
| EditorTableFix | 修复表格结构 |

### EditorCharacterCount

字符/单词计数，通过 `.Characters` 和 `.Words` 命名空间子组件访问。

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| children | 前缀内容（如 "字数："） | `ReactNode` | - |
| className | 附加 className | `string` | - |

### 导出类型

组件同时 re-export：`Editor`、`JSONContent`（来自 @tiptap/react）、`SuggestionItem`。

<h2 id="faq">常见问题</h2>

### 如何设置编辑器的初始内容？

使用 EditorProvider 的 `content` 属性。可以是 HTML 字符串或 ProseMirror JSON 对象：

```tsx
<EditorProvider content="<p>Hello World</p>" />
```

### 如何获取编辑器内容变更？

使用 `onUpdate` 回调：

```tsx
<EditorProvider
  onUpdate={({ editor }) => {
    const html = editor.getHTML()
    const json = editor.getJSON()
  }}
/>
```

### 如何限制输入的字符数？

设置 `limit` 属性并配合 `EditorCharacterCount` 展示计数：

```tsx
<EditorProvider limit={500}>
  <EditorCharacterCount.Characters />
</EditorProvider>
```

### 斜杠命令如何触发？

在编辑器中输入 `/` 自动触发斜杠命令面板，支持模糊搜索。默认命令包括：Text、Heading 1/2/3、Bullet List、Numbered List、To-do List、Quote、Code、Table。

<h2 id="llm-rules">LLM 使用规则</h2>

1. 从 `@/components/ui/editor` 按需导入 EditorProvider 及所需子组件。
2. EditorProvider 作为顶层容器，所有菜单子组件放在其内部。
3. 文本格式按钮和节点切换按钮放在 EditorBubbleMenu 内，EditorBubbleMenu 会自动在子元素间插入 Separator。
4. 表格操作按钮放在对应的 Table Menu 容器内（GlobalMenu / ColumnMenu / RowMenu）。
5. 需要字符限制时设置 `limit`，配合 `EditorCharacterCount.Characters` 或 `.Words` 展示计数。
6. 编辑器默认注册 StarterKit + 代码高亮 + 斜杠菜单 + 表格 + 任务列表，如需额外扩展通过 `extensions` 属性传入。
7. 不要引入 Ant Design 相关依赖。

<h2 id="source">源码</h2>
<SourceViewer componentId="editor" />
