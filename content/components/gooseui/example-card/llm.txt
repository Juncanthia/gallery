---
component: ExampleCard
slug: gooseui/example-card

import: import { ExampleCard } from "@/components/ui/example-card"

usage: |
  <ExampleCard />

props:
  无 props。ExampleCard 是一个自包含的 Login 卡片示例组件，不接收任何外部参数。

constraints:
  - 纯展示组件，渲染一个包含 Email、Password 输入框和 Sign In 按钮的 Login 卡片。
  - 样式通过独立的 CSS 文件（example-card.css）实现，使用 CSS 自定义属性。
  - 使用原生 HTML form 元素，不依赖任何表单库。
  - 内部使用固定的 CSS class 命名（BEM 风格），不要在外部覆盖这些 class。
  - 不要为登录表单引入 react-hook-form、Formik 等表单库。
  - 不要引入 Ant Design 相关依赖。

source: src/components/gooseui/components/blocks/example-with-css/example-card.tsx
example: content/components/gooseui/example-card/examples/basic.tsx
docs: content/components/gooseui/example-card/index.mdx
---
