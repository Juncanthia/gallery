Spinner 复合组件。

<ComponentShowcase id="spinner" path="spinner/basic" />

```tsx
import { Spinner } from "@/components/ui/spinner"

export default function Demo() {
  return <Spinner className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/spinner/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/spinner` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="spinner" />
