CreditCard 复合组件。

<ComponentShowcase id="credit-card" path="credit-card/basic" />

```tsx
import { CreditCard } from "@/components/ui/credit-card"

export default function Demo() {
  return <CreditCard className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/credit-card/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/credit-card` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="credit-card" />
