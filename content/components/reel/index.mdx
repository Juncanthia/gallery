Reel 复合组件。

<ComponentShowcase id="reel" path="reel/basic" />

```tsx
import { Reel } from "@/components/ui/reel"

export default function Demo() {
  return <Reel className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/reel/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/reel` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="reel" />
