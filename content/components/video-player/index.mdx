VideoPlayer 复合组件。

<ComponentShowcase id="video-player" path="video-player/basic" />

```tsx
import { VideoPlayer } from "@/components/ui/video-player"

export default function Demo() {
  return <VideoPlayer className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/video-player/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/video-player` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="video-player" />
