AvatarStack 复合组件。

<ComponentShowcase id="avatar-stack" path="avatar-stack/basic" />

```tsx
import { AvatarStack } from "@/components/ui/avatar-stack"

export default function Demo() {
  return <AvatarStack className="w-full max-w-sm" />
}
```

<h2 id="api">API</h2>

API 以 `src/components/blocks/avatar-stack/index.tsx` 中的 TypeScript 类型为准。

<h2 id="llm-rules">LLM 使用规则</h2>
1. 从 `@/components/ui/avatar-stack` 导入。

<h2 id="source">源码</h2>
<SourceViewer componentId="avatar-stack" />
