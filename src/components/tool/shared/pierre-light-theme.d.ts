// Type stub for the pierre-light shiki theme (JSON re-exported as JS).
// The theme is consumed in code-block.tsx via `as never`, so a loose shape is enough
// to satisfy module resolution without TS7016 (no declaration file).
declare const pierreLightTheme: {
  name: string
  type: string
  [key: string]: unknown
}
export default pierreLightTheme
