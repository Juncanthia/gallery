// Type stub for the pierre-dark shiki theme (JSON re-exported as JS).
// The theme is consumed in code-block.tsx via `as never`, so a loose shape is enough
// to satisfy module resolution without TS7016 (no declaration file).
declare const pierreDarkTheme: {
  name: string
  type: string
  [key: string]: unknown
}
export default pierreDarkTheme
