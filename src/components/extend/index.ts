// Extend UI components are consumed via deep imports:
//   import { PDFViewer } from "@/components/extend/components/ui/pdf-viewer"
// This barrel is intentionally empty (same convention as other @hyper/* UI packages).
export {}
